{
  "name": "now-go-modules",
  "version": 2,
  "builds": [
    { "src": "api/handlers/health.go", "use": "@now/go@canary" }
  ],
  "routes": [
    { "src": "/health", "dest": "api/handlers/health.go" }
  ]
}
